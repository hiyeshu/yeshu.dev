# src/
> L2 | 父级: ../CLAUDE.md

成员清单
App.jsx: React 根页面组件，编排 yeshu.dev 居中标题与 ScrollVelocity 背景动效。
main.jsx: React 浏览器入口，挂载 App 并加载全局样式。
styles.css: 全局样式层，定义主题、居中布局和主页级组件覆写。
components/: 复用组件目录，当前承载 React Bits ScrollVelocity。

法则: 成员完整·一行一文件·父级链接·技术词前置

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
